import React from 'react';

// const Home = React.lazy(
//   (): Promise<{ default: React.ComponentType }> => import('./HomePage'),
// );
function Home() {
  console.log('Main');
}

export default Home;
